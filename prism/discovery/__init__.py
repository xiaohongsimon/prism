"""Discovery layer — auto-populate signal sources from external behavior signals.

Currently houses X follows discovery (via bird CLI). Future: LLM-driven source
proposal (Phase 2 full), auto-trial new sources (Phase 3).
"""
